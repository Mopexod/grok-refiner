# Project Context

> Этот файл содержит минимальный контекст проекта для AI.
> Обновляется при значимых изменениях архитектуры или направления.

---

## Проект

**Название:** Grok Refiner

**Описание:** Chrome Extension — Control Surface для Grok. Позволяет пинить фрагменты ответов AI, редактировать комментарии и отправлять обратно в чат по одному, не теряя контекст.

**Целевая аудитория:** Пользователи Grok (grok.com, x.ai, x.com/i/grok), работающие с большими AI-ответами и нуждающиеся в точечном управлении контекстом

**Язык интерфейса:** Смешанный (требует унификации: popup на английском, панель на русском)

---

## Технологический стек

### Frontend
- Framework: Vanilla TypeScript
- UI: Shadow DOM + inline styles
- State management: Chrome Storage API + локальное состояние
- Build tool: TypeScript Compiler

### Extension
- Type: Chrome Extension, Manifest V3
- Permissions: `storage`, `clipboardWrite`
- Content Scripts: работают на `grok.com/*`, `x.ai/*`, `x.com/*`
- Architecture: Content Script + Iframe Panel + Floating Button

### Infrastructure
- Platform: Chrome Web Store (потенциально)
- Version control: Git + GitHub
- Package manager: npm

---

## Локальная разработка

### Установка и запуск
```bash
# Установка зависимостей
npm install

# Компиляция TypeScript
npm run build
# или для watch-режима:
npm run watch

# Загрузка расширения в Chrome
1. Открыть chrome://extensions/
2. Включить "Developer mode"
3. "Load unpacked" → выбрать папку /public
4. Открыть grok.com и проверить работу
```

### Тестирование изменений
1. Внести изменения в `.ts` файлы
2. `npm run build` (или автоматически при watch)
3. Обновить расширение: chrome://extensions/ → кнопка reload
4. Перезагрузить страницу grok.com (F5)
5. Проверить функционал

### Главный файл для начала работы
- `src/content-script.ts` — точка входа, инициализация всего функционала

---

## Архитектура

### Структура проекта
```
/src
  /adapters      - платформенные адаптеры (grokAdapter, detectPlatform)
  /ui            - UI компоненты (panel, floatingButton)
  /types         - TypeScript типы (EditItem, StoredState, PlatformId)
  /utils         - утилиты (buildChatText, storage)
  content-script.ts  - главный скрипт, инжектируемый в Grok
  panel-main.ts      - логика iframe-панели
/public
  manifest.json  - конфигурация расширения
  popup.html     - popup при клике на иконку
  panel.html     - iframe правой панели
```

### Зоны частых изменений
**Эти файлы меняются чаще всего:**
- `src/content-script.ts` — основная логика выделения текста и событий
- `src/adapters/grokAdapter.ts` — DOM селекторы Grok (ломаются при обновлениях Grok UI)
- `src/ui/panel.ts` — логика панели редактирования
- `src/types/index.ts` — TypeScript контракты и интерфейсы
- `public/manifest.json` — permissions и скрипты расширения

### Ключевые паттерны
- **Adapter Pattern** для кросс-платформенности (Grok, потенциально Claude)
- **Shadow DOM** для изоляции стилей UI компонентов
- **Message Passing** между content-script и iframe-панелью
- **Observer Pattern** для отслеживания выделения текста и новых сообщений AI

---

## Важные архитектурные решения

### Решение 1: Iframe-панель вместо встраивания в DOM
**Что:** Правая панель реализована через iframe с `panel.html`, а не прямой инжект в DOM страницы  
**Почему:** Полная изоляция от стилей Grok/X, предотвращение конфликтов CSS и JS. Grok активно обновляет UI, прямая инжекция приводила к поломкам  
**Последствия изменения:** Потеря изоляции стилей → конфликты с Tailwind и styled-components Grok  
**Не менять без согласования**

### Решение 2: Adapter-слой для платформ
**Что:** Вся логика работы с DOM конкретной платформы изолирована в `adapters/grokAdapter.ts`  
**Почему:** Возможность поддержки Claude и других платформ без изменения core-логики  
**Последствия изменения:** Жесткая привязка к Grok → невозможность расширения на другие AI  
**Не менять без согласования**

### Решение 3: Плавающая кнопка по выделению
**Что:** Кнопка "Add Edit" появляется только при выделении текста внутри AI-сообщения  
**Почему:** Естественный UX, не загромождает интерфейс, контекстно-зависимый триггер  
**Последствия изменения:** UX деградация, пользователь не поймет как добавлять правки  
**Не менять без согласования**

---

## Соглашения по коду

### Naming conventions
- Типы/Интерфейсы: `PascalCase` (EditItem, StoredState)
- Функции: `camelCase` (loadState, handleAddEdit)
- Константы: `UPPER_SNAKE_CASE` (STORAGE_KEY, DEFAULT_STATE)
- Файлы: `camelCase.ts` (grokAdapter.ts, content-script.ts)

### Code style
- Отступы: пробелы, 2 символа
- Quotes: одинарные ('string')
- Semicolons: yes
- Line length: 100

### Комментарии
- Язык комментариев: английский
- Обязательны для: сложной логики селекторов, platform-specific хаков, workarounds для Grok DOM

---

## Текущее состояние

### Завершено
- ✅ Content Script с детектором платформы
- ✅ Iframe-панель со списком правок
- ✅ Плавающая кнопка по выделению
- ✅ Сохранение состояния в chrome.storage
- ✅ Отправка одной правки в Grok input
- ✅ Grok Adapter (grok.com, x.ai, x.com/i/grok)

### В работе
- 🔄 **Унификация UI языка**
  - Проблема: popup на английском ("Grok Refiner"), панель на русском ("Редактор простыни")
  - Решение: TBD — решить какой язык оставить (требует обсуждения)
  - Файлы: `public/popup.html`, `public/panel.html`, `src/ui/panel.ts`
  
- 🔄 **Устранение дублирования floatingButton логики**
  - Проблема: плавающая кнопка реализована дважды (ui/floatingButton.ts и inline в content-script.ts)
  - Решение: удалить inline-реализацию, использовать модуль ui/floatingButton.ts
  - Файлы: `src/content-script.ts`, `src/ui/floatingButton.ts`

### Известные проблемы
- ⚠️ **Несогласованность нейминга**: popup говорит "Grok Refiner", панель — "Редактор простыни"
- ⚠️ **Дублирование UI**: плавающая кнопка реализована дважды (ui/floatingButton.ts и inline в content-script.ts)
- ⚠️ **Мультиплатформенная модель без реализации**: типы поддерживают 'grok' | 'claude', но Claude-адаптер отсутствует

### Технический долг
- [ ] Удалить одну из реализаций floating button, использовать модуль
- [ ] Перевести всю панель на английский или унифицировать с popup
- [ ] Удалить или реализовать Claude-поддержку (сейчас мёртвый код)
- [ ] Добавить error handling при вставке в Grok input (contenteditable может не сработать)
- [ ] Обработка случаев, когда Grok обновляет DOM и ломает селекторы

---

## Запрещенные изменения (без обсуждения)

- ❌ **Убрать iframe-панель** (замена на direct DOM injection)  
  Причина: потеря изоляции стилей → конфликты с Grok CSS

- ❌ **Изменить scope доменов в manifest.json** без проверки на всех вариантах Grok  
  Причина: расширение перестанет работать на части платформ (x.ai, x.com/i/grok)

- ❌ **Изменить структуру StoredState**  
  Причина: **breaking change** → данные существующих пользователей станут нечитаемыми

- ❌ **Убрать Shadow DOM из UI компонентов**  
  Причина: потеря изоляции стилей → кнопки и панель будут styled неправильно

- ❌ **Изменить формат buildChatText** без тестирования на реальных ответах Grok  
  Причина: Grok может не распарсить новый формат → функционал сломается

---

## Примеры данных

### StoredState (chrome.storage.local)
```json
{
  "platform": "grok",
  "editItems": [
    {
      "id": "1705420800000",
      "text": "Объясни квантовую запутанность простыми словами",
      "comment": "Сделать короче и добавить примеры",
      "timestamp": 1705420800000
    },
    {
      "id": "1705420900000",
      "text": "В контексте машинного обучения...",
      "comment": "Уточнить про supervised learning",
      "timestamp": 1705420900000
    }
  ]
}
```

### Формат buildChatText (отправляется в Grok)
```
[Edit 1/3]
"Объясни квантовую запутанность простыми словами"

Сделать короче и добавить примеры
```

### Grok DOM структура (актуальна на январь 2026)

**AI message container:**
```html
<div data-testid="message-container">
  <div data-testid="message-text">
    AI response text here...
  </div>
</div>
```

**Chat input:**
```html
<div contenteditable="true" data-testid="chat-input">
  User input...
</div>
```

### Проверенные селекторы (работают на всех доменах)

**⚠️ КРИТИЧНО:** Эти селекторы найдены методом проб и тестирования. Не меняйте без причины!

**AI message container:**
```javascript
// Primary (работает на grok.com, x.ai, x.com/i/grok)
const messageSelector = 'div[data-testid="message-container"]';

// Fallback если Grok обновит DOM:
const messageFallback = 'article[role="article"]:has(div[data-testid="message-text"])';

// Использование в коде:
const message = document.querySelector(messageSelector) || 
                document.querySelector(messageFallback);
```

**Chat input area:**
```javascript
// Primary
const inputSelector = 'div[contenteditable="true"][data-testid="chat-input"]';

// Fallback
const inputFallback = 'div[contenteditable="true"]:not([aria-label*="Search"])';

// Проверка что input существует:
const input = document.querySelector(inputSelector) || 
              document.querySelector(inputFallback);
if (!input) {
  console.error('Grok input not found - DOM structure changed');
}
```

**Text content внутри AI message:**
```javascript
// Для выделения текста внутри AI ответа
const textContent = message.querySelector('[data-testid="message-text"]');
```

### Known selector issues (решены)

1. **Проблема:** На x.com/i/grok другие classNames чем на grok.com
   **Решение:** Используем data-testid вместо классов

2. **Проблема:** contenteditable может быть у search input тоже
   **Решение:** Fallback исключает элементы с aria-label содержащим "Search"

3. **Проблема:** При обновлении страницы DOM перестраивается
   **Решение:** MutationObserver отслеживает изменения (см. content-script.ts)

### Тестирование селекторов

При изменении/добавлении селекторов **обязательно** проверить на:
- ✅ grok.com
- ✅ x.ai  
- ✅ x.com/i/grok

Процесс тестирования:
1. Открыть DevTools → Console
2. Запустить: `document.querySelector('[ваш_селектор]')`
3. Убедиться что находит правильный элемент
4. Проверить на всех трёх доменах

**⚠️ Внимание:** Селекторы могут ломаться при обновлениях Grok UI. Последняя проверка: 16 января 2026

---

## Полезные ссылки

- Репозиторий: [добавить URL после создания GitHub repo]
- Chrome Extension Manifest V3 Docs: https://developer.chrome.com/docs/extensions/mv3/
- Grok UI селекторы: см. `src/adapters/grokAdapter.ts`
- Task tracker: [добавить URL если используется]

---

## Быстрый старт для AI

После прочтения этого файла ты должен знать:
1. Как запустить проект локально (`npm install` → `npm run build` → load unpacked)
2. Где находится главная логика (`src/content-script.ts`)
3. Какие файлы меняются чаще всего (см. "Зоны частых изменений")
4. Что нельзя трогать (см. "Запрещенные изменения")
5. Текущие задачи (см. "В работе")

**Подтверди готовность фразой:**  
"Правила загружены, контекст Grok Refiner понят"

Затем кратко перечисли:
- Текущие "В работе" задачи
- Known issues
- Запрещенные изменения

---

**Последнее обновление:** 16 января 2026  
**Версия проекта:** pre-release (в разработке)
